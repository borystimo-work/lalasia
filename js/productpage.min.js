const templates=document.querySelectorAll(".color-detail-product__template");templates.forEach(e=>{e.addEventListener("click",()=>{templates.forEach(c=>c.classList.remove("--active")),e.classList.add("--active")})});
